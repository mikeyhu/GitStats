name: platform 
location: ~/casper/core/platform
max: 0
one_per_day: true
output: json
collect:
 #count all tests in .scala files
 unit-tests: 'find src/test/unit -iname *.scala | xargs -I{} grep "it(\"" {} | wc -l'

